public class sonarQube05 {
    public int doDivide(int num, boolean isTrue){
        int divisor = 0;
        if (isTrue) divisor = 20;
        else divisor = 1;
        return num / divisor;
    }
}
